import os
import sys
import wave

from openal import al


# load and store a wav file into an openal buffer
class SoundLoader:
    def __init__(self, filename):
        # load/set wav file
    
    # delete loaded sound
    def delete(self):
